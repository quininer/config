#!/usr/bin/sh

cat << EOF
{
	"set-env": {
		"EDITOR": "helix"
	},
	"push-path": [
		"$HOME/.local/bin",
		"$HOME/.cargo/bin"
	],
	"alias": {
		"ls": "exa",
		"la": "exa -hal",
		"wget": "curl -L -O -C -"
	}
}
EOF
